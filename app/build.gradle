apply plugin: 'com.android.application'

android {
    signingConfigs {
        Cloud59 {
            keyAlias 'Cloud59'
            keyPassword 'Bubbs.2003'
            storeFile file('/home/rcheung/Desktop/Catholicism/Cloud59APKey.keystore')
            storePassword 'Bubbs.2003'
        }
    }
    compileSdkVersion 20
    buildToolsVersion "20.0.0"
    defaultConfig {
        applicationId 'ca.cloud59.SanctissimissaTu'
        minSdkVersion 15
        targetSdkVersion 15
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.Cloud59
    }
    buildTypes {
        release {
            runProguard true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.Cloud59
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:20.0.0'
}
